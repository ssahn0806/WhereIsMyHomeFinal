<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.house.model.dao.SidoCodeDAO">
	<select id="selectSidoCode" parameterType="string" resultType="SidoCode">
		select sidoName from sidocode where sidoCode=#{sidoCode}
	</select>
	
	<select id="selectSidoCodes" resultType="SidoCode">
		select sidoCode,sidoName from sidocode
	</select>
</mapper>














