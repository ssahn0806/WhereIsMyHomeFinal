<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.house.model.dao.UserDAO">
	<select id="login" parameterType="map" resultType="string">
		select username from user where userid=#{userid} and userpw=#{userpw}
	</select>
	
	<insert id="register" parameterType="User">
		insert into user(userid, userpw, username, userlevel, userfavdongcode) 
		values(#{userid},#{userpw},#{username},#{userlevel},#{userfavdongcode})
	</insert>
	
	<update id="modify" parameterType="User">
		update user set userpw = #{userpw}, username = #{username} where userid=#{userid}
	</update>
	
	<delete id="delete" parameterType="string">
		delete from user where userid=#{userid}
	</delete>
	
	<select id="getUser" parameterType="string" resultType="User">
		select userid,userpw,username,userlevel,userfavdongcode from user where userid=#{userid}
	</select>
	
	<select id="findPassword" parameterType="map" resultType="string">
		select userpw from user where userid=#{userid} and username=#{username}
	</select>
	
	<update id="registerFavorite" parameterType="map">
		update user set userfavdongcode=#{dongCode} where userid=#{userid}
	</update>
</mapper>














