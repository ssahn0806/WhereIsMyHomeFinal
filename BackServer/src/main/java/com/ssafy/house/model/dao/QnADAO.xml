<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.house.model.dao.QnADAO">
	<select id="qnaList" resultMap="com.ssafy.resultmaps.QnAMap">
		select article_no, userid, subject, content, hit, register_time from qna order by article_no desc
	</select>
	
	<select id="qnaView" parameterType="int" resultMap="com.ssafy.resultmaps.QnAMap">
		select article_no, userid, subject, content, hit, register_time from qna where article_no=#{articleNo}
	</select>
	
	<update id="updateHit" parameterType="int">
		update qna set hit = hit+1 where article_no=#{articleNo}
	</update>
	
	<insert id="insert" parameterType="QnA">
		insert into qna(userid, subject, content, hit, register_time)
		values(#{userId},#{subject},#{content},0,now())
	</insert>
	
	<delete id="delete" parameterType="int">
		delete from qna where article_no=#{articleNo}
	</delete>
	
	<update id="modify" parameterType="qna">
		update qna set subject=#{subject},content=#{content}
		where article_no=#{articleNo}
	</update>
</mapper>














